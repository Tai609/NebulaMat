# Optional NebulaMat overlay for the shipped DSH `web` profile.
# The DSH executable owns loader/profile composition and the complete bottom
# loop. Project-owned scientific modules are mounted by the desktop host from
# plugins.json; this overlay intentionally adds no duplicate core rows.
[]
