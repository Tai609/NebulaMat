interface:
  display_name: "UMA Adsorption Energy Screening"
  short_description: "Compute and audit same-model adsorption energies"
  default_prompt: "Use $uma-adsorption-energy-screening to compute a provenance-checked UMA adsorption energy from a slab, adsorbate, and adsorbed slab."

policy:
  allow_implicit_invocation: true
