interface:
  display_name: "MatterGen Structure Generation"
  short_description: "Generate and audit constrained crystal candidates"
  default_prompt: "Use $mattergen-structure-generation to generate candidate crystal structures with parameter preflight and standardization."

policy:
  allow_implicit_invocation: true
