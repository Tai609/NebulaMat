interface:
  display_name: "MatterSim Relaxation and Screening"
  short_description: "Relax structures and audit MatterSim energy proxies"
  default_prompt: "Use $mattersim-relaxation-screening to relax the standardized structure and record MatterSim energy, force, stress, and provenance."

policy:
  allow_implicit_invocation: true
